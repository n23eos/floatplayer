# План: стабилизация (фиксы багов, найденных ревизией)

Ветка: feat/live-support-and-pip-fixes

1. pip-controller.js — защита open():
   - флаг isOpening против параллельных вызовов (двойной Alt+P);
   - проверка pipWindow.closed после каждого await — не переносить плеер
     в уже закрытое окно (иначе плеер пропадает со страницы);
   - pagehide вешаем сразу после requestWindow.
   → verify: двойной Alt+P открывает одно окно; закрытие окна во время
     открытия не теряет плеер.

2. pip-controller.js — snapToVideoAspect: не повторять resizeTo с тем же
   целевым размером (окно упёрлось в минимум → вечный цикл каждые 300 мс).
   → verify: узкое окно не дёргается бесконечно.

3. sponsor-block.js — сегменты присваивать только если loadedKey ещё
   актуален (гонка при быстрой смене видео).
   → verify: быстрая навигация не оставляет чужие сегменты.

4. settings.js — колбэки onChange в try/catch: упавший слушатель не
   ломает остальных.

5. pip-related.js — клик по рекомендации: если anchor отвязан от DOM
   (сайдбар перерисован), фолбэк через поиск ссылки по videoId.

6. pip-comments.js — не ставить пустой avatar.src (лишний запрос).

7. Тесты: npm test — все проходят.

8. Убраны аудио-пресеты (night/voice/bass): пользы мало, кода много.
   Граф Web Audio упрощён до source → gain (усиление 0–300% осталось).
   Вычищены: селект в панели, настройка audioPreset, стили, ключи локалей
   (14 языков), упоминания в описаниях магазина и на сайте.
   → verify: ссылок на preset в коде нет, тесты проходят.

9. Название видео в PiP-окне: рамку Chrome не поменять (там только origin),
   поэтому своя строка .ytfp-title у верхнего края окна, панель сдвинута
   ниже (top: 8px → 30px). Обновляется тем же MutationObserver, что и
   document.title.
   → verify: макет отрисован headless Chrome — название видно и в
     компактном режиме (панель скрыта), и в узком окне шортсов
     (обрезается многоточием, панель переносится ниже).
